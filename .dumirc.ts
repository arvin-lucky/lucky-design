import { defineConfig } from 'dumi';

export default defineConfig({
  base: "/lucky-design",
  publicPath: "/lucky-design",
  outputPath: 'docs-dist',
  themeConfig: {
    name: 'lucky-design',
  },
});
